# OpenPalette — environment variables
# Local-first app: no environment variables required for development.
# Vite exposes vars prefixed with NEXT_PUBLIC_ to the client.

# Production URL (used for meta tags)
# NEXT_PUBLIC_SITE_URL=https://openpalette.vercel.app
